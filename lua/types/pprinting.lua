--- @class pretty
pretty = {
    --- Pretty print all arguments with a newline seperator
    --- @param ... any List of values of any type to print
    print = function(...)end,
    --- Stringify the argument in a human readable format
    --- @param arg any Any type of value to format into a string
    stringify = function(arg)end
}